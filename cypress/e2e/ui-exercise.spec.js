describe('UI Exercise', () => {
  it('includes at least one JavaScript Test', () => {
    cy.visit('/')
  })

  it('uses a color scheme similar to that of Gmail', () => {
    cy.visit('/')
  })

  it('displays a list of emails with name, title, date and tags', () => {
    cy.visit('/')
  })

  it('allows for selection of one or more emails', () => {
    cy.visit('/')
  })

  it('allows users to perform a number of actions on one or more selected emails', () => {
    cy.visit('/')
  })

  it('shows a sidebar that displays a number of tags', () => {
    cy.visit('/')
  })

  it('table cells should be viewable at all resolutions above 320px', () => {
    cy.visit('/')
  })
})

// findBy
// findAllBy
// queryBy
// queryAllBy
